# 機能仕様テンプレ（日本語UI固定）

## 概要
- 機能名：
- 対象ユーザー：
- 目的（ユーザー価値）：

## 画面一覧
- 画面名：
  - パス：
  - 画面見出し（日本語・固定）：

## UI文言一覧（日本語・固定）
> テストはこの文言を `name` として使用するため、実装で言い換えない。

### ボタン
- [ ] 作成
- [ ] 保存
- [ ] 更新
- [ ] 削除
- [ ] キャンセル
- [ ] 閉じる
- [ ] 再試行

### ラベル
- （例）メールアドレス
- （例）パスワード

### メッセージ
- 成功トースト：（例）「保存しました」
- 422時：（例）「入力内容を確認してください」
- 500時：（例）「サーバーで問題が発生しました」
- 空状態：（例）「データがありません」

## 受け入れ条件（Given/When/Then）
### 正常系
- Given:
- When:
- Then:

### 異常系：入力エラー（422）
- Given:
- When:
- Then:
  - フィールドエラーが日本語で表示される
  - 入力値が保持される

### 異常系：サーバエラー（500）
- Given:
- When:
- Then:
  - 画面上部に日本語のエラーバナーが表示される
  - 「再試行」ボタンで再実行できる

## API契約（OpenAPI参照）
- 利用エンドポイント：
- 成功時レスポンス：
- 422レスポンス（フィールドエラー形式）：
- 500レスポンス：

## 仕様メモ
- Query invalidation方針：
- 画面遷移方針：